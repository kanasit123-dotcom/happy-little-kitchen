# แผนพัฒนาร้านอาหารและคณิตศาสตร์

> ปรับปรุง 2026-09-23 หลังตรวจเทียบกับโค้ดจริงของทั้งสอง repo และตกลงกับผู้ปกครองแล้ว — อ่านหัวข้อ 0 ก่อนเสมอ
> ไฟล์นี้ย้ายจาก `game-lilly` มาอยู่ใน repo ครัวแล้ว (Phase 0) เพราะฟีเจอร์พัฒนาที่นี่

## 0. การตัดสินใจที่ตกลงแล้ว (2026-09-23)

| # | เรื่อง | ข้อสรุป |
| --- | --- | --- |
| D1 | ตอบไม่ตรง | อนุญาตให้ "ลองอีกครั้ง" แบบนุ่มนวล **เฉพาะโหมดร้านและตลาด** แบบเดียวกับตั้งเลขในโลกของลิลลี่: สั่นเบาๆ พร้อมคำชวนลองใหม่และคำใบ้ภาพ ห้ามมี ✕ สีแดง หน้าเศร้า เสียง buzz ตัวนับจำนวนที่ผิดบนจอ หรือการลงโทษใดๆ ต้องแก้กฎใน `MEMORY.md` (บรรทัด "Never add … wrong-answer states") และ `AI_HANDOFF.md` ของครัวให้เขียนข้อยกเว้นนี้ไว้ |
| D2 | เงินในเกม | เงินที่ขายได้เก็บลงกระปุก แล้วพาไป **ตลาด** ซื้อของตกแต่งร้าน เด็กได้เล่นทั้งบทคนขาย (รับเงิน ทอนเงินให้ลูกค้า) และบทคนซื้อ (จ่ายเงิน นับเงินทอนที่ได้รับ) มีกติกากันแรงกดดันในหัวข้อ 14 |
| D3 | จุดคำนวณ | **ไม่เกิน 1 จุดคำนวณต่อออร์เดอร์หรือต่อการซื้อ 1 ครั้ง** เลือกตามระดับ ส่วนที่เหลือระบบคิดให้ และลูกค้าหรือคนขายพูดให้ฟัง |
| D4 | ที่เก็บข้อมูล | ร้านใช้ localStorage key แยก `happy-little-kitchen-shop-v1` ไม่ฝังใน `happy-little-kitchen-v1` |
| D5 | ลำดับงาน | ทำชิ้นเล็กที่ลิลลี่เล่นได้จริงก่อน (หยิบของ + รับเงิน + ทอนแบบนับต่อ ตัวเลขไม่เกิน 10) ยังไม่ต้องย้าย column engine ส่งให้เล่นแล้วฟัง feedback ก่อนทำส่วนที่ยากขึ้น |

## 1. เป้าหมาย

เพิ่มเกมร้านอาหารสำหรับเด็กอายุประมาณ 5 ขวบ (อนุบาล 2–3 ซึ่งเคยเล่นตั้งบวก ตั้งลบ ตัวทด ตัวยืมในโลกของลิลลี่มาแล้ว) โดยต่อยอดจากสองโปรเจกต์ที่มีอยู่:

- `game-lilly`: โลกของลิลลี่ มีระบบตั้งบวก ตั้งลบ ตัวทด ตัวยืม เสียง และการเก็บความคืบหน้า
- `happy-little-kitchen`: ครัวจิ๋วแสนสนุก มีเมนูอาหาร ขั้นตอนทำอาหาร ตัวละคร ออร์เดอร์ อารมณ์ตัวละคร รูป และเสียง

หัวใจของเกมคือ "เพื่อนมาเป็นลูกค้าซื้ออาหาร" ไม่ใช่แบบฝึกคณิตศาสตร์ที่แยกออกจากเรื่องราว เด็กหยิบอาหาร รับเงิน และทอนเงินจริงภายในเรื่องเดียวกัน โดยออร์เดอร์หนึ่งฝึกทักษะคณิตศาสตร์เพียงอย่างเดียวตามระดับ (D3) แล้วนำเงินที่ขายได้ไปซื้อของตกแต่งร้านที่ตลาด (D2)

ลูกค้าหนึ่งคนควรจบได้ในราว 1–1.5 นาที

ห้ามเขียนเกมใหม่ทั้งหมด ห้ามลบหรือเปลี่ยนพฤติกรรมของเกมทำอาหาร การป้อนเพื่อน ด่านเรียน และความคืบหน้าเดิม

## 2. ข้อสรุปทางสถาปัตยกรรม

### 2.1 เจ้าของฟีเจอร์

ระบบร้านอาหารและตลาดพัฒนาใน repo `happy-little-kitchen` เพราะมีอาหาร ตัวละคร ขั้นตอนทำอาหาร ออร์เดอร์ สมุดผลงาน และระบบเสียงอยู่แล้ว

หลังร้านเสถียร ให้เพิ่มปุ่ม "ร้านอาหาร" ในห้องพักเล่นของ `game-lilly` เพื่อเปิดหน้าร้านของครัวในแท็บเดิม พร้อมปุ่มกลับสู่โลกของลิลลี่ (Phase 7)

### 2.2 สิ่งที่ไม่ควรทำ

- ไม่คัดลอกเกมครัวทั้งหมดเข้า `game-lilly` เพราะจะทำให้ไฟล์ออฟไลน์ซ้ำและติดตั้งครั้งแรกหนักขึ้นมาก
- ไม่ import `game-lilly/js/games/column.js` ข้ามเว็บไซต์โดยตรง เพราะผูกกับ CSS เสียง ตัวละคร และ state ของโลกของลิลลี่
- ไม่ใช้ iframe สำหรับเกมหลัก เพราะการสัมผัส เสียง การย้อนกลับ และโหมดออฟไลน์จะซับซ้อน
- ไม่รวม localStorage ของสองเกมเข้าด้วยกัน และไม่ฝังข้อมูลร้านในเซฟครัว (D4)
- ไม่ import โมดูลใหม่โดยไม่มี `?v=` เดียวกับ `app.js` (ดู 2.4)

### 2.3 การนำระบบคณิตศาสตร์เดิมมาใช้

นำตรรกะจาก `game-lilly/js/games/column.js` มาปรับเป็นโมดูลในครัว โดยคงหลักการเดิม:

- ตั้งบวกและตั้งลบแนวตั้ง คำนวณหลักหน่วยก่อนหลักสิบ
- การมัด 10 หน่วยเป็น 1 สิบ ตัวทด ตัวยืม
- แป้นตัวเลข
- สรุปวิธีทำและเสียงอ่านหลังจบ

สิ่งที่ต้องเปลี่ยน:

- รับโจทย์ที่กำหนดจากออร์เดอร์ได้ ไม่สุ่มเองเสมอ และส่งผลลัพธ์กลับไปยังออร์เดอร์เดิม
- ใช้ตั้งเลขแนวตั้งเฉพาะโจทย์ที่มีเลขสองหลัก โจทย์ที่ทุกตัวไม่เกิน 10 ใช้การนับภาพหรือนับเหรียญ (หัวข้อ 6)
- ข้อความทุกประโยคต้องมีทั้งไทยและอังกฤษ (ตัวเดิมเขียนไทยตายตัวในโค้ด)

### 2.4 โครงสร้างโมดูล

`js/app.js` ของครัวเป็นโมดูลเดียวยาว ~2,550 บรรทัด ไม่มี export จึงห้ามให้โมดูลใหม่ import กลับเข้าไป (จะเกิด circular import) ให้แยกดังนี้:

| ไฟล์ | หน้าที่ |
| --- | --- |
| `js/shop/core.js` | logic ล้วน: สร้างออร์เดอร์ เลือกจุดคำนวณ คำนวณราคา/ทอน/เหรียญ commit ธุรกรรม normalize ข้อมูล — ไม่แตะ DOM และไม่เรียก localStorage เอง จึงทดสอบด้วย `node --test` ได้ |
| `js/shop/config.js` | ราคา จำนวนต่อรอบ เกณฑ์ปลดล็อกระดับ รายการของตกแต่ง |
| `js/shop/ui.js` | หน้าร้าน หน้าตลาด ถาด เหรียญ กระปุก |
| `js/shop/column.js` | engine ตั้งบวก/ตั้งลบที่ปรับจากโลกของลิลลี่ |
| `css/shop.css` | UI ทั้งหมด scope ใต้ `.shop` |

`app.js` ส่งสิ่งที่ร้านต้องใช้เป็น object เดียวตอนเปิดร้าน เช่น `{ speak, stopSpeech, sfx, t, local, art, friendSrc, dishSrc, screen, startRecipe, showHome, popup, confetti, getLang }`

โหลดร้านแบบ dynamic import ตอนแตะปุ่มร้านเท่านั้น และต่อ `?v=` เดียวกับ `app.js`:

```js
const V = new URL(import.meta.url).search; // เช่น "?v=32"
const shop = await import(`./shop/ui.js${V}`);
```

โมดูลลูกใน `js/shop/` ก็ใช้วิธีเดียวกันต่อกันไป เหตุผล: `sw.js` ตอบจาก cache ก่อน และ GitHub Pages cache ไฟล์ไว้ 10 นาที ถ้าโมดูลไม่มี `?v=` อาจได้ `app.js` ใหม่คู่กับไฟล์ร้านเก่า ทุก URL ของโมดูลต้องอยู่ในรายการของ `sw.js` แบบตรงตัว รวม `?v=`

## 3. สิ่งที่ระบบเดิมรองรับแล้ว

### โลกของลิลลี่

- `js/games/column.js`: ตัวทด ตัวยืม บล็อกฐานสิบ การตอบทีละหลัก และสรุปโจทย์ ตอบผิดได้ไม่จำกัดโดยสั่นเบาๆ และพูด "ลองอีกทีนะ"
- `js/audio.js`: เสียงที่คืน Promise และรอจนอ่านจบ กดฟังซ้ำได้
- `design/voice.py`: สแกนข้อความทุกไฟล์ใน `js/` อัดเลข 0–100 อ่าน `+ − =` เป็นคำ ตัดคลิปที่อัดไม่ได้ และมี phrase chunk สำหรับโจทย์ปัญหา
- `tests/game.cjs`: ทดสอบหน้าเดิม เซฟเดิม responsive และ offline (รันได้เฉพาะ local)

### ครัวจิ๋วแสนสนุก

- 9 เมนูอาหารและครัวอิสระ ขั้นตอนทำอาหารจริง 16 ชนิด
- เพื่อน 11 ตัวพร้อมความชอบและรูปอารมณ์ ระบบออร์เดอร์รายวันแบบ `เพื่อน + เมนู` ป้อนเพื่อนได้หลายตัว
- สมุดผลงานและรูปอาหารที่เด็กตกแต่งเอง
- เสียงไทยอัดล่วงหน้า (Premwadee) เล่นผ่าน Web Audio และเสียงประกอบสังเคราะห์
- localStorage key `happy-little-kitchen-v1` พร้อมการย้ายเซฟรุ่นเก่า
- จำนวนที่เด็กเห็นตอนทำอาหาร: คุกกี้ปั้น 6 ก้อน (`shape count: 6`), คัพเค้กอบในถาด 6 หลุม (`cupcakes-baked.png`), พิซซ่าตัด 2 ครั้งได้ 4 ชิ้น (`slice count: 2`)

## 4. ช่องว่างที่ต้องพัฒนา

ด้านเกม:

- ไม่มีสต็อก ราคาต่อชิ้น จำนวนสินค้าในออร์เดอร์ เงินที่ลูกค้าจ่าย เงินทอน กระปุก ตลาด และประวัติธุรกรรม
- ไม่มีรูปเหรียญ ธนบัตร เคาน์เตอร์ ชั้นวาง ถาด พิซซ่า 1 ชิ้น และของตกแต่งร้าน

ด้านเทคนิคที่พบจากการอ่านโค้ด:

- `loadState()` ของครัวอ่านเฉพาะ field ที่ระบุชื่อ field ใหม่จะหายตอนโหลด และถ้า parse ล้มเหลว `catch` จะคืน state ว่างแล้ว `saveState()` เขียนทับ ทำให้สมุดผลงานหาย → เหตุผลของ D4
- `design/voice.py` ของครัวอ่านเฉพาะ `js/app.js` ข้อความในไฟล์ใหม่จะไม่ถูกอัด แล้วตกไปใช้เสียงเครื่อง
- `sw.js` ตอบจาก cache ก่อน → ต้องต่อ `?v=` ให้ทุกโมดูล (2.4)
- column engine เดิม:
  - รับเฉพาะกติกาสุ่มโจทย์
  - ตัวตั้งหลักเดียวแสดงเป็น `08` และถามหลักสิบว่า `1 + 0 = ?` (เช่น `8 + 7`)
  - คำตอบหลักเดียวจากเลขสองหลักแสดงเป็น `07` (เช่น `12 − 5`, `20 − 13`)
  - ข้อความเป็นภาษาไทยอย่างเดียว ใช้เครื่องหมายลบ `−` (U+2212)
  - ยังไม่มีโหมดคิดเอง ปุ่มขอความช่วยเหลือ และการสาธิตหลังตอบไม่ตรงซ้ำ
- ชุดทดสอบเดิมยังไม่ได้เล่นโจทย์ตัวทด/ตัวยืมจนจบ
- เปิดร้านครั้งแรกสต็อกเป็น 0 เด็กต้องทำอาหารครบหนึ่งเมนูก่อนเจอลูกค้า → แจกของเริ่มต้น (หัวข้อ 9)

## 5. ประสบการณ์ผู้เล่นในร้าน

หน้าร้านนำเสนอทีละงาน ไม่ทำเป็น dashboard ข้อมูลเต็มจอ ทุกหน้าใช้งานได้จากภาพและเสียง เด็กไม่ต้องอ่านข้อความ

วงจรหนึ่งออร์เดอร์:

1. เพื่อนเดินหรือเลื่อนเข้ามาที่เคาน์เตอร์
2. เพื่อนพูดออร์เดอร์ พร้อมรูปอาหารและจำนวน เช่น `คัพเค้ก × 2`
3. เด็กหยิบอาหารจากชั้นลงถาดให้ครบ (ของบนชั้นลดลงให้เห็นทันที)
4. ป้ายราคาแสดงยอดรวม — ถ้าระดับนี้ไม่ได้ฝึกการบวกราคา ลูกค้าพูดยอดให้ฟังเลย เช่น "ทั้งหมด 14 บาทนะ"
5. ลูกค้าวางเงินบนเคาน์เตอร์
6. ถ้ามีเงินทอน เด็กทอนด้วยเหรียญแบบนับต่อ (หัวข้อ 13)
7. ลูกค้ารับอาหาร ยิ้ม ขอบคุณ มีแอนิเมชันฉลอง และเหรียญหยอดลงกระปุก
8. ระบบตัดสต็อกและบันทึกยอดขายในครั้งเดียว (หัวข้อ 11)

**จุดคำนวณมีเพียง 1 จุดต่อออร์เดอร์ (D3)** ระดับเป็นตัวกำหนดว่าออร์เดอร์นี้จะฝึกข้อไหน ข้ออื่นระบบคิดให้แล้วลูกค้าพูดให้ฟัง ชนิดของจุดคำนวณ:

| ชนิด | เด็กทำอะไร | ตัวอย่าง |
| --- | --- | --- |
| `count` | หยิบอาหารให้ครบจำนวน | คุกกี้ 4 ชิ้น |
| `collect` | หยิบเหรียญจากกระเป๋าลูกค้าให้พอดีกับราคา | ราคา 7 → หยิบ 5 + 2 |
| `change` | ทอนแบบนับต่อด้วยเหรียญ | ราคา 7 จ่าย 10 → ทอน 3 |
| `price` | บวกราคารวม | `7 + 7`, `8 + 7` |
| `remaining` | ของบนชั้นเหลือกี่ชิ้น | `6 − 2 = 4` |
| `restock` | ของเดิมรวมกับที่เพิ่งทำเพิ่ม | `2 + 6 = 8` (ถามตอนกลับจากทำอาหาร) |

เรื่อง "ของเหลือ" ไม่ถามทุกออร์เดอร์ เพราะลูกค้าไม่เกี่ยวกับสต็อกและจะทำให้เรื่องสะดุด ปกติให้เห็นจำนวนบนชั้นเฉยๆ และถามเป็นครั้งคราวในฐานะจุดคำนวณของออร์เดอร์นั้น

เด็กออกจากร้านได้ทุกเมื่อ ลูกค้าบอก "ไว้มาต่อนะ" และออร์เดอร์ค้างไว้ให้ทำต่อ

## 6. วิธีคำนวณ โหมด และการตอบไม่ตรง

### 6.1 วิธีแสดงโจทย์ตามขนาดตัวเลข

| ขนาดโจทย์ | วิธีหลัก | เมื่อขอความช่วยเหลือ |
| --- | --- | --- |
| ทุกตัวไม่เกิน 10 | นับภาพอาหารหรือเหรียญ แตะทีละชิ้นพร้อมเสียงนับ | ไฮไลต์ทีละชิ้นและนับให้ฟัง |
| มีเลขตั้งแต่ 10 ขึ้นไป | แป้นตัวเลข (คิดเอง) | ตั้งเลขแนวตั้งแบบ guided พร้อมตัวทด ตัวยืม และภาพฐานสิบ |
| ทอนเงิน | นับต่อด้วยเหรียญ | ระดับ 5 ขึ้นไป เปิดตั้งลบแบบ guided ได้ |

### 6.2 โหมด

ใช้ชื่อเดียวกันทุกที่ทั้ง state และ API:

- `independent` (คิดเอง): แสดงโจทย์และแป้นตัวเลขหรือของให้นับ ยังไม่แสดงวิธีทำ
- `guided` (ขอความช่วยเหลือ): เปิดทีละขั้นพร้อมภาพ ปุ่มขอความช่วยเหลือกดได้ตลอดและเปิดทันที

### 6.3 การตอบไม่ตรง (ตาม D1)

- ครั้งแรก: อยู่โจทย์เดิม สั่นเบาๆ พูด "ลองนับอีกครั้งนะ" และแสดงคำใบ้ด้วยรูปอาหารหรือเหรียญ
- ครั้งที่สอง: เปิด `guided` ของโจทย์เดิมโดยอัตโนมัติ
- ห้าม ✕ สีแดง หน้าเศร้า เสียง buzz ตัวนับจำนวนครั้งที่ผิดบนจอ ห้ามหักเงิน ห้ามเปลี่ยนลูกค้า ห้ามสุ่มโจทย์ใหม่
- จำนวนครั้งที่ตอบไม่ตรงเก็บไว้ภายในสำหรับหน้าผู้ปกครองเท่านั้น

ผลลัพธ์จาก math module:

```js
{
  answer: 15,
  attempts: 2,
  usedHelp: true,
  completed: true
}
```

## 7. สัญญาของ math module

สร้าง `happy-little-kitchen/js/shop/column.js` โดยปรับจาก engine เดิม:

```js
solveColumn(host, {
  problem: { a: 8, op: '+', b: 7, result: 15 },
  mode: 'independent',
  context: 'price',
  hintItems: []
}, hooks)
```

ข้อกำหนด:

- รับ fixed problem จากออร์เดอร์ได้ และยังสุ่มโจทย์ได้สำหรับทดสอบหรือโหมดฝึก
- `op` รับทั้ง `'-'` และ `'−'` แล้วแปลงเป็น `'−'` ภายใน (ตรงกับ engine เดิมและ `spokenForm()`)
- ตัวตั้งหลักเดียว เช่น `8 + 7` ไม่แสดง `0` ในหลักสิบ และไม่ถาม `1 + 0` ให้ยกตัวทดลงมาเลย ("ทด 1 ยกลงมาเป็น 1")
- ไม่แสดงศูนย์นำหน้าคำตอบ: `12 − 5 = 7` และ `20 − 13 = 7` ต้องไม่แสดง `07`
- รองรับ `+` และ `−` โดยไม่สร้างเลขติดลบ
- ข้อความไทย/อังกฤษเทียบเท่ากัน ตามภาษาที่เลือกในครัว
- แยก `buildSteps(problem)` เป็นฟังก์ชันล้วนที่ทดสอบด้วย node ได้
- คืน Promise เมื่อเสียงและสรุปโจทย์จบแล้ว
- ยกเลิกได้เมื่อออกจากหน้า โดยไม่บันทึกผลสำเร็จปลอม
- กดฟังโจทย์ซ้ำได้

โจทย์บังคับที่ต้องมี automated test:

- `8 + 7 = 15` มีตัวทด ไม่มี `0` หน้าตัวตั้ง
- `12 − 5 = 7` มีตัวยืม ไม่แสดงศูนย์นำหน้า (ทดสอบ engine เท่านั้น เพราะไม่ใช่การจ่ายเงินที่เป็นธรรมชาติ)
- `20 − 13 = 7` มีตัวยืม ไม่แสดงศูนย์นำหน้า
- `6 − 2 = 4` ลบหลักเดียวสำหรับสต็อก (ใช้การนับภาพ ไม่ใช่ตั้งเลข)

## 8. สินค้า ราคา และการจ่ายเงิน

### 8.1 สินค้า MVP

จำนวนต่อรอบต้องเท่ากับที่เด็กเห็นตอนทำอาหาร:

| เมนู | หน่วยขาย | ทำหนึ่งรอบได้ | ราคาต่อหน่วย | ที่มาของจำนวน |
| --- | --- | ---: | ---: | --- |
| คุกกี้ | ชิ้น | 6 | 5 บาท | ขั้นปั้น 6 ก้อน |
| คัพเค้ก | ชิ้น | 6 | 7 บาท | ถาดอบ 6 หลุม |
| พิซซ่า | ชิ้น | 4 | 8 บาท | ตัด 2 ครั้ง ได้ 4 ชิ้น |

หลัง MVP จึงเพิ่มเมนูอื่น ราคาเป็นจำนวนเต็ม ไม่มีสตางค์

### 8.2 ลูกค้าจ่ายเงินแบบคนจริง

- จ่ายพอดี: ลูกค้ายื่นกระเป๋าเงินให้เด็กหยิบเหรียญให้ครบราคา (จุดคำนวณ `collect`)
- จ่ายเกิน: ลูกค้าจ่ายด้วยเหรียญหรือธนบัตร **ใบเดียว** ที่มากกว่าราคา — ยอดไม่เกิน 10 ใช้เหรียญ 10, ยอด 11–20 ใช้แบงก์ 20
- ไม่สร้างการจ่ายแปลกๆ เช่น จ่าย 12 บาทซื้อของ 5 บาท
- ยอดรวมไม่เกิน 20 บาทในช่วงแรก เพิ่มแบงก์ 50/100 และยอดถึง 99 ภายหลัง

### 8.3 โจทย์ที่เกิดขึ้นเองจากราคาชุดนี้

| สถานการณ์ | โจทย์ |
| --- | --- |
| คุกกี้ 1 ชิ้น จ่าย 10 | `10 − 5 = 5` |
| คัพเค้ก 1 ชิ้น จ่าย 10 | `10 − 7 = 3` |
| พิซซ่า 1 ชิ้น จ่าย 10 | `10 − 8 = 2` |
| คุกกี้ 2 ชิ้น | `5 + 5 = 10` |
| คัพเค้ก 2 ชิ้น | `7 + 7 = 14`, จ่าย 20 ทอน `6` |
| พิซซ่า 2 ชิ้น | `8 + 8 = 16`, จ่าย 20 ทอน `4` |
| พิซซ่า + คัพเค้ก | `8 + 7 = 15`, จ่าย 20 ทอน `5` |
| พิซซ่า + คุกกี้ | `8 + 5 = 13`, จ่าย 20 ทอน `7` |
| มีคุกกี้ 6 ขาย 2 | `6 − 2 = 4` |
| เหลือคุกกี้ 2 ทำเพิ่ม 6 | `2 + 6 = 8` |

## 9. การเติมสต็อกด้วยเกมทำอาหารเดิม

เพิ่ม context ตอนเริ่มทำอาหาร:

```js
startRecipe(recipeId, {
  destination: 'stock',
  returnTo: 'shop',
  restockId: 'restock-...'
})
```

กติกา:

- `destination: 'serve'` (ค่าเริ่มต้น) ใช้พฤติกรรมเดิมทุกอย่าง
- `destination: 'stock'` เล่นขั้นทำอาหารและตกแต่งเดิม แต่เมื่อเสร็จให้เพิ่มสต็อกแทนหน้าเสิร์ฟเพื่อน
- ห้ามแก้ array ขั้นตอนใน `RECIPES` แบบถาวร ให้เลือกปลายทางจาก session context
- เติมสต็อกครั้งเดียวต่อ `restockId` แม้แตะปุ่มซ้ำหรือ reload ช่วงจบ (เก็บ `lastRestockId`)
- บันทึกรูปอาหารลงสมุดผลงานได้ แต่ไม่เพิ่ม `served` และไม่ถือว่าเป็นการป้อนเพื่อน
- ชั้นวางแสดงรูปอาหารแบบปกติ (MVP) — ไอเดียภายหลัง: ติดรูปที่เด็กแต่งเองเป็นป้าย "เมนูพิเศษของร้าน"
- กลับมาจากการทำอาหารแล้วให้เปิดออร์เดอร์เดิมต่อทันที
- ถ้าระดับนี้ใช้จุดคำนวณ `restock` ให้ถาม "มี 2 ชิ้น ทำเพิ่ม 6 ชิ้น รวมเป็นกี่ชิ้น?" ตอนกลับเข้าร้าน

ของเริ่มต้น: เปิดร้านครั้งแรกให้มีคุกกี้ 6 ชิ้นบนชั้นเลย (ครั้งเดียว บันทึก `seeded: true`) ลูกค้าคนแรกจะมาทันที แล้วค่อยพาไปทำอาหารเมื่อของใกล้หมด

## 10. โครงสร้างข้อมูลร้าน

เก็บแยกที่ key `happy-little-kitchen-shop-v1` (D4):

```js
{
  schema: 1,
  seeded: true,
  level: 1,
  ordersDone: 0,
  purchasesDone: 0,
  preferredMode: 'independent',
  stock: { cookie: 6, cupcake: 0, pizza: 0 },
  piggy: 0,
  totals: { sales: 0, customerPaid: 0, changeGiven: 0, spent: 0 },
  activeOrder: null,
  activePurchase: null,
  lastRestockId: null,
  transactions: [],
  decor: { owned: [], placed: {} }
}
```

- `piggy` คือเงินในกระปุก เก็บเป็นจำนวนบาท หน้าจอแปลงเป็นเหรียญเอง
- ค่าคงที่ที่ต้องเป็นจริงเสมอ: `piggy === totals.sales − totals.spent` และ `totals.customerPaid − totals.changeGiven === totals.sales`
- `transactions` เก็บล่าสุด 100 รายการ ทั้งขาย (`type: 'sell'`) และซื้อ (`type: 'buy'`)
- `loadShop()` normalize ทุก field ถ้า parse ไม่ผ่าน ให้ย้ายค่าเดิมไปเก็บที่ `happy-little-kitchen-shop-broken` แล้วเริ่มร้านใหม่ **ห้ามแตะ key ของครัว**
- `loadState()` ของครัวไม่ต้องแก้
- หน้าผู้ปกครองเพิ่มปุ่ม "ล้างร้าน" แยกจากการล้างครัว (กดค้างยืนยันแบบเดิม)

ตัวอย่าง `activeOrder`:

```js
{
  id: 'order-...',
  customer: 'seal',
  lines: [{ recipe: 'cupcake', qty: 1, unitPrice: 7 }],
  total: 7,
  checkpoint: 'change',
  status: 'picking',
  picked: 0,
  payment: {
    offered: [10],
    paid: 10,
    change: 3,
    changeCoins: []
  },
  math: { answer: null, attempts: 0, usedHelp: false },
  mistakes: 0
}
```

`status` เดินตามลำดับ `arriving → picking → checkpoint → paying → change → done` บันทึกทุกครั้งที่เปลี่ยน เพื่อ reload แล้วกลับมาที่ขั้นเดิม (ตัวเลขที่พิมพ์ค้างไว้ไม่ต้องเก็บ ให้เริ่มขั้นนั้นใหม่)

## 11. กติกาธุรกรรม

การขายและการซื้อเป็น atomic transaction:

1. สร้างออร์เดอร์จากสต็อกที่มีจริง
2. ยังไม่ตัดสต็อกระหว่างหยิบหรือคำนวณ
3. ทำจุดคำนวณของออร์เดอร์นี้ให้ครบ
4. ตรวจผลรวมเหรียญทอน
5. ตรวจสต็อกอีกครั้ง และตรวจว่า `transactions` ยังไม่มี `id` นี้ (idempotency key คือ `order.id`)
6. สร้าง object ร้านใหม่ทั้งก้อน: บันทึก transaction ยอดเงิน ตัดสต็อก `activeOrder = null`
7. `localStorage.setItem` ครั้งเดียว
8. แล้วจึงแสดงฉลอง

ก่อน commit:

- ออกจากหน้าได้โดยออร์เดอร์ยังค้างอยู่
- reload แล้วกลับสู่ออร์เดอร์เดิมได้
- แตะปุ่มยืนยันซ้ำต้องไม่ขายซ้ำ

เมื่อ commit การขาย:

```text
totals.sales        += ราคาสินค้า
totals.customerPaid += เงินที่ลูกค้าจ่าย
totals.changeGiven  += เงินที่ทอน
piggy               += ราคาสินค้า
```

เมื่อ commit การซื้อ:

```text
totals.spent += ราคาของตกแต่ง
piggy        -= ราคาของตกแต่ง   (ห้ามติดลบ)
decor.owned  += ของชิ้นนั้น
```

## 12. การสร้างออร์เดอร์

- สุ่มเฉพาะสินค้าที่มีสต็อกเพียงพอ
- จำนวนที่ขอไม่เกินสต็อกและไม่เกินระดับปัจจุบัน
- เลือกจุดคำนวณ 1 ชนิดจากรายการของระดับ ไม่ใช้ชนิดเดิมติดกันเกิน 2 ครั้ง
- หลีกเลี่ยงออร์เดอร์ซ้ำแบบเดิมติดกัน
- ถ้าไม่มีสินค้า ให้เพื่อนบอกอย่างเป็นมิตรและแสดงปุ่มทำอาหารเพิ่ม
- เก็บออร์เดอร์เดิมจนกว่าจะขายสำเร็จหรือผู้ปกครองยกเลิก
- `state.order` เดิมของเกมครัวคงไว้สำหรับออร์เดอร์รายวันเดิม ออร์เดอร์ร้านใช้ `activeOrder` ของร้านเท่านั้น

## 13. เงินและเหรียญ

### 13.1 เหรียญและธนบัตร

- เหรียญ 1, 2, 5, 10 บาท
- แบงก์ 20 บาท (ใช้ตั้งแต่ระดับ 5 ขึ้นไป) — ภายหลังเพิ่ม 50 และ 100

ภาพต้องแยกกันได้โดยไม่ต้องอ่าน: ตัวเลขใหญ่ ขนาดและสีต่างกันตามของจริง (1 เงินเล็ก, 2 สีทอง, 5 เงินขอบทองแดง, 10 ทองวงเงินใหญ่สุด, 20 ธนบัตรสีเขียว) ห้ามมีพระบรมฉายาลักษณ์ และห้ามลอกแบบธนบัตรจริง

รูปเหรียญ ธนบัตร และป้ายราคาวาดแบบ**ไม่มีตัวเลข** (Gemini เขียนตัวเลขเพี้ยนบ่อย) แล้วเกมวางตัวเลขทับด้วย CSS ให้คมและถูกต้องเสมอ

กระปุกออมสินเป็น**รูปแมวน้ำ** ห้ามเป็นรูปหมู (กติกาเดิมของทั้งสองเกม: ไม่มีหมี หมู ฮิปโป หมา โคอาลา)

### 13.2 ทอนแบบนับต่อ

แม่ค้าจริงทอนโดยนับต่อจากราคาไปจนถึงเงินที่ลูกค้าให้ เช่น ราคา 7 ลูกค้าให้ 10:

1. ลูกค้าพูด "ของ 7 บาท ให้ 10 บาทนะ"
2. เด็กแตะเหรียญจากลิ้นชักลงมือลูกค้า
3. ทุกเหรียญที่วาง ตัวนับพูดยอดต่อจากราคา: วาง 2 บาท → "เก้า" วาง 1 บาท → "สิบ ครบแล้ว!"
4. แตะเหรียญในมือลูกค้าเพื่อหยิบคืนได้

วิธีนี้รวมขั้น "คำนวณเงินทอน" กับ "เลือกเหรียญ" เป็นขั้นเดียว เหมาะกับอนุบาล การตั้งลบเก็บไว้เป็นปุ่มช่วยตั้งแต่ระดับ 5

ลิ้นชักเหรียญทอนในร้านมีเหรียญไม่จำกัด (ไม่ต้องจัดการเหรียญย่อยหมด) ส่วนรายได้สุทธิ (= ราคาสินค้า) หยอดลงกระปุก

### 13.3 ผลตอบรับ

- ทอนครบ: ลูกค้ายิ้ม ขอบคุณ รับอาหาร และฉลอง
- ทอนยังไม่ครบ: ลูกค้าทำท่าสงสัย มี `?` และพูดว่า "ลองนับอีกครั้งนะ เงินยังไม่ครบนะ"
- ทอนเกิน: ลูกค้าทำท่าประหลาดใจ มี `!` และพูดว่า "เยอะไปนิด ลองตรวจอีกครั้งนะ"

MVP ใช้รูปตัวละครเดิมร่วมกับ CSS animation และสัญลักษณ์ ยังไม่ต้องสร้างรูปอารมณ์ใหม่

## 14. ตลาด — ซื้อของตกแต่งร้าน (D2)

### 14.1 วงจรการซื้อ

1. จากหน้าร้าน แตะกระปุก → ไปตลาด (เพื่อนตัวหนึ่งเป็นคนขาย เช่น กระรอก)
2. ของตกแต่งวางบนแผงพร้อมป้ายราคาตัวเลขใหญ่ แตะแล้วได้ยินชื่อและราคา
3. เด็กเลือกของ 1 ชิ้น
4. กระเป๋าเงินแสดงเงินในกระปุกเป็นเหรียญ โดยแตกเหรียญให้จ่ายพอดีกับของที่เลือกได้เสมอเมื่อเงินพอ
5. เด็กหยิบเหรียญวางบนเคาน์เตอร์ตลาด:
   - ยังไม่พอ: คนขายพูด "ยังไม่ครบนะ ขาดอีก X บาท"
   - พอดี: คนขายรับเงิน ยิ้ม
   - เกิน: คนขายทอนโดยนับต่อให้ฟังทีละเหรียญ
6. ของเข้ากล่องของตกแต่ง แล้วเด็กแตะช่องในร้านเพื่อวาง
7. commit การซื้อตามหัวข้อ 11

### 14.2 จุดคำนวณในตลาด (1 จุดต่อการซื้อ ตามระดับร้าน)

| ระดับ | เด็กทำอะไร |
| --- | --- |
| 1 | แตะเหรียญจ่ายทีละเหรียญ คนขายนับให้ฟังจนครบ |
| 2–3 | เลือกเหรียญให้พอดีกับราคา |
| 4 ขึ้นไป | จ่ายเหรียญใหญ่ แล้วนับเงินทอนที่ได้รับว่ากี่บาท |

### 14.3 กติกากันแรงกดดัน

- ราคาของตกแต่ง 5–20 บาท ขาย 1–3 ออร์เดอร์ก็ซื้อได้ 1 ชิ้น
- ของทุกชิ้นมีขายตลอด ไม่มีลดราคาจำกัดเวลา ไม่มีกล่องสุ่ม ไม่มีของหายหรือหมดอายุ
- เงินติดลบไม่ได้ ถ้าเงินไม่พอ คนขายบอกอย่างอ่อนโยนว่า "เก็บเงินอีก X บาทนะ" และแสดงเป็นภาพเหรียญที่ขาด ไม่มีนาฬิกาหรือการเร่ง
- ไม่มีการแจ้งเตือนชวนกลับมาเล่น ไม่มีการเชื่อมกับเงินจริง
- วางของ ย้ายช่อง หรือเก็บของคืนกล่องได้ฟรีตลอด

### 14.4 ของตกแต่ง MVP (8–10 ชิ้น)

ผ้าปูโต๊ะ, แจกันดอกไม้, ป้ายร้าน, ต้นไม้กระถาง, โคมไฟ, กันสาดสี, ธงราว, ลูกโป่ง, พรมหน้าร้าน, เบาะเก้าอี้

วางแบบช่องที่กำหนดไว้ (`decor.placed = { slotId: itemId }`) ไม่ใช่ลากวางอิสระ เพื่อให้หน้าร้านยังพอดีจอ 390×664

## 15. ระดับความยาก

ปลดล็อกระดับตามจำนวนออร์เดอร์ที่ขายสำเร็จ (ค่าใน `config.js` ปรับได้) ไม่อิงความแม่นยำ และไม่ลดระดับเมื่อตอบไม่ตรง เรียงระดับให้ตัวเลขไม่เกิน 10 มาก่อน เพื่อให้ชิ้นแรก (D5) ทำได้โดยยังไม่ต้องใช้ column engine

| ระดับ | ปลดล็อก | ออร์เดอร์ | จุดคำนวณ (สุ่ม 1) | การจ่ายเงิน |
| --- | ---: | --- | --- | --- |
| 1 นับอาหาร | เริ่ม | 1 เมนู 1–5 ชิ้น | `count` | ระบบรับเงินพอดีให้ |
| 2 รับเงินพอดี | 5 ออร์เดอร์ | 1 เมนู 1 ชิ้น | `collect`, `count` | จ่ายพอดี |
| 3 ทอนเงิน | 10 ออร์เดอร์ | 1 เมนู 1 ชิ้น | `change`, `collect` | เหรียญ 10 |
| 4 บวกราคา | 16 ออร์เดอร์ | 1 เมนู 2 ชิ้น | `price`, `remaining`, `restock` | จ่ายพอดี |
| 5 หลายเมนู | 24 ออร์เดอร์ | 2 เมนู รวมไม่เกิน 20 | `price`, `change`, `remaining` | แบงก์ 20 |
| 6 บริหารร้าน | 32 ออร์เดอร์ | ตามสต็อก | ทุกชนิด | ทุกแบบ |

- ระดับ 1–3 ใช้การนับภาพและเหรียญทั้งหมด (Phase 1)
- ระดับ 4–5 ใช้ column engine เมื่อโจทย์มีเลขตั้งแต่ 10 ขึ้นไป (Phase 4–5)
- ระดับ 6: ดูสต็อกทุกเมนู เลือกทำอาหารเพิ่ม และดูของตกแต่งทั้งหมด
- ตลาดเปิดตั้งแต่ขายสำเร็จครั้งแรก ไม่ต้องรอระดับ 6
- ไม่มีโฆษณา การซื้อด้วยเงินจริง หรือแรงกดดันให้เล่นต่อ

## 16. เสียง

- ทุกประโยคต้องรอเสียงจบก่อนเปลี่ยนขั้นหรือเปลี่ยนลูกค้า ประโยคสั้นเพื่อให้ลูกค้าหนึ่งคนไม่ยืดเกินไป
- ใช้ Premwadee สำหรับภาษาไทยตามระบบเดิม
- นำความสามารถจาก `game-lilly/design/voice.py` มาใช้ใน voice.py ของครัว:
  - สแกนทุกไฟล์ใน `js/` รวม `js/shop/` (ไม่ใช่แค่ `app.js`)
  - อัดเลข 0–100
  - `spokenForm()` อ่าน `+ − =` เป็นคำ
  - ตัดคลิปที่อัดไม่ได้ออกจาก manifest
  - phrase chunk สำหรับประโยคที่ประกอบบ่อย: "ทั้งหมด … บาท", "ให้ … บาท", "ทอน … บาท", "ขาดอีก … บาท", "รวมเป็นกี่ชิ้น", "เหลือกี่ชิ้น", "ครบแล้ว", "ขอบคุณนะ"
- เมื่อปรับ voice.py ตัวหนึ่ง ให้ปรับอีกตัวให้ตรงกัน
- ห้ามใช้ `<audio>` สำหรับเสียงพูด เพราะเคยทำให้เสียง Web Audio ของเครื่องครัวเงียบบน iOS
- ภาษาอังกฤษต้องทำงานเทียบเท่าภาษาไทย MVP ยังใช้ device voice ได้
- ปุ่มฟังซ้ำอ่านเฉพาะโจทย์หรือคำสั่งปัจจุบัน

## 17. รูปที่ต้องสร้าง (`design/PROMPTS-gemini-5.md`)

ผู้ปกครองสร้างรูปใน Gemini เอง โควต้าจำกัด จึงรวมหลายชิ้นต่อ sheet แล้วตัดด้วย `design/blobs.py --grid RxC` และ `design/cutout.py` (เพิ่ม prefix `coin-`, `shop-`, `decor-`)

เขียนแล้วใน Phase 0 — รวม 4 รูป:

- Sheet A (4×4): เหรียญ 1, 2, 5, 10, แบงก์ 20, กระปุกแมวน้ำ, ลิ้นชักเงิน, ถาดเสิร์ฟ, ป้ายราคาเปล่า, พิซซ่า 1 ชิ้น, ถุงใส่ของ, กระเป๋าเงิน, ชั้นวาง, กริ่งเคาน์เตอร์, กล่องของตกแต่ง, กระดานเมนู
- Sheet B (2×5): ของตกแต่ง 10 ชิ้นตามหัวข้อ 14.4
- ฉากหน้าร้าน `shop.jpg` และฉากแผงตลาด `market.jpg` (แนวนอน 3:2 เหมือน `kitchen.jpg`)

`design/cutout.py` รองรับ prefix `coin-` → `assets/coins/`, `shop-` → `assets/shop/`, `decor-` → `assets/decor/` แล้ว

Phase 1 เริ่มได้โดยวาดเหรียญด้วย CSS (วงกลมสีพร้อมตัวเลข) ระหว่างรอรูป จะได้ไม่ติดรอ

## 18. ไฟล์ที่คาดว่าจะเพิ่มหรือแก้ไข

### Repo `happy-little-kitchen`

- `js/app.js`: ปุ่มเข้าร้านบน home, dynamic import ร้าน, ส่ง `api` object, session context `destination: 'stock'`, ปุ่มล้างร้านในหน้าผู้ปกครอง
- `js/shop/core.js`, `js/shop/config.js`, `js/shop/ui.js`, `js/shop/column.js`: ตามหัวข้อ 2.4
- `css/shop.css`: UI ร้าน ตลาด เหรียญ สต็อก และ math overlay scope ใต้ `.shop`
- `index.html`: โหลด `css/shop.css?v=`
- `design/voice.py`: สแกนทุกไฟล์ ตัวเลข วลีราคาและเงินทอน
- `design/PROMPTS-gemini-5.md`: รูปร้าน เหรียญ และของตกแต่ง
- `sw.js`: cache ไฟล์ รูป และเสียงใหม่ (URL ตรงตัวรวม `?v=`) พร้อม bump version
- `tests/shop/*.test.mjs`: unit test ของ core และ column (รันด้วย `node --test`)
- `tests/app.cjs` หรือ `tests/shop.cjs`: e2e ของร้านและตลาด + regression ของเกมครัวเดิม
- `MEMORY.md` และ `AI_HANDOFF.md`: บันทึก D1–D5 และกติกาที่ตกลงแล้ว

ปุ่มเข้าร้านบน home ต้องไม่ทำให้หน้าจอ 390×664 ต้องเลื่อน (มี media query ย่อ home สำหรับจอเตี้ยอยู่แล้ว) ให้เลือกตำแหน่งตอน Phase 1 พร้อม screenshot ทุกขนาด

### Repo `game-lilly` หลังร้านเสถียร

- `js/screens/mini.js`: tile ร้านอาหารแบบเปิดได้เสมอ
- `css/lilly.css`: รูปแบบ tile และสถานะกลับจากร้าน
- `tests/game.cjs`: ตรวจ launcher และยืนยันความคืบหน้าเดิมไม่เปลี่ยน
- `sw.js`: bump version เฉพาะเมื่อ shell เปลี่ยน
- `MEMORY.md`: ระบุว่าร้านอาหารดูแลใน repo ครัว

## 19. แผนพัฒนาเป็นระยะ

### Phase 0: Baseline และกติกา

- ย้ายหรือคัดลอกไฟล์แผนนี้ไปไว้ใน repo ครัว (ฟีเจอร์อยู่ที่นั่น)
- รัน tests ของทั้งสอง repo ก่อนแก้
- แก้ `MEMORY.md` และ `AI_HANDOFF.md` ของครัวตาม D1–D4
- เขียน `design/PROMPTS-gemini-5.md` ให้ผู้ปกครองเริ่มสร้างรูปคู่ขนานไปได้เลย
- บันทึกค่า localStorage ตัวอย่างของครัวก่อนเริ่ม

เกณฑ์ผ่าน: tests เดิมผ่าน กติกาใหม่อยู่ใน MEMORY แล้ว

**สถานะ: เสร็จแล้ว (2026-09-23)**

- ย้ายแผนมาไว้ใน repo ครัวแล้ว
- baseline: `node tests/app.cjs` (ครัว v31) ผ่าน และ `node tests/game.cjs` (ลิลลี่) ผ่านครบ 6 ชุด
- `MEMORY.md` และ `AI_HANDOFF.md` มีข้อยกเว้น D1 และหัวข้อร้านอาหารแล้ว
- `design/PROMPTS-gemini-5.md` พร้อมให้สร้างรูป และ `design/cutout.py` รองรับ `coin-`/`shop-`/`decor-`
- เซฟครัวตัวอย่าง `tests/fixtures/kitchen-save-v31.json` (โหลดในแอปแล้วไม่มี error) ใช้ทดสอบว่าร้านไม่แตะ key ของครัว

### Phase 1: ร้านชิ้นแรกที่เล่นได้ (ระดับ 1–3)

- `shop/core.js` + `shop/config.js` + key แยก + ของเริ่มต้นคุกกี้ 6 ชิ้น
- ปุ่มเข้าร้านบน home (dynamic import + `?v=`)
- เคาน์เตอร์ ลูกค้าเดินเข้า speech bubble
- หยิบของลงถาด (`count`) รับเงินพอดี (`collect`) ทอนแบบนับต่อ (`change`)
- ผลตอบรับแบบ D1 ทอนน้อย/เกิน/ครบ
- กระปุก และ atomic commit ที่กันขายซ้ำ
- voice.py รุ่นใหม่ ตัวเลขและวลีเงิน
- เหรียญ CSS ระหว่างรอรูป
- unit tests ของ core + e2e วงจรขาย + regression ครัว

เกณฑ์ผ่าน: ลิลลี่เล่นตั้งแต่ลูกค้าเข้าร้านจนขายสำเร็จได้บน iPad จริง → deploy แล้วรอ feedback ก่อนทำ Phase ต่อไป

**สถานะ: เสร็จแล้ว พร้อมกับ Phase 2 (2026-09-23, v32)** — ทำ Phase 2 ไปด้วยเลย เพราะถ้าไม่มีการเติมสต็อก คุกกี้ 6 ชิ้นขายหมดใน 2 ออร์เดอร์แล้วร้านก็เล่นต่อไม่ได้

- ไฟล์: `js/shop/core.js` (logic ล้วน + ค่าตั้งต้น — **รวม config ไว้ในไฟล์นี้** แทน `config.js` แยก เพื่อไม่ต้อง import ต่อกันหลายชั้นแบบไม่มี `?v=`), `js/shop/ui.js`, `css/shop.css`
- ปุ่ม "ร้านของหนู" อยู่คู่กับครัวอิสระบนหน้าครัว หน้าผู้ปกครองมีสถิติร้าน + ปุ่มล้างร้าน (ล้างทั้งหมดก็ล้างร้านด้วย)
- ระดับ 1 นับของ (ลูกค้าจ่ายพอดีให้เอง), ระดับ 2 รับเงินพอดีจากกระเป๋าลูกค้า, ระดับ 3 ทอนแบบนับต่อ; ตอบไม่ตรงครั้งแรก = ลูกค้าทำ ?/! + คำใบ้, ครั้งที่สอง = โหมดช่วย (ช่องมีเลข / เหรียญเรืองแสง / เส้นนับ); ปุ่ม 💡 ช่วยหน่อย เปิดโหมดช่วยทันที
- เติมสต็อก: ปุ่ม "＋ ทำเพิ่ม" บนชั้น หรือหน้า "ของหมด" → ทำอาหารตามขั้นเดิม → หน้าจบแสดงของที่ได้ + เก็บรูปในสมุด (ไม่นับว่าป้อนเพื่อน) → กลับร้าน ออร์เดอร์เดิมรออยู่
- เสียง: `design/voice.py` อ่าน `js/shop/*.js` + อัดเลข 0–100 และหลักร้อย/พันถ้วน (เงินในกระปุก 135 พูดเป็น "100 35")
- tests: `node --test "tests/shop/*.test.mjs"` (logic + ทุกประโยคไทยมีคลิปครบ) และ `node tests/shop.cjs` (e2e ใน Chrome)
- ยังไม่ทำ: จุดคำนวณ `restock` (ระดับ 4), ตลาด (Phase 3), column engine (Phase 4)

### Phase 2: เติมสต็อกด้วยการทำอาหาร

- `destination: 'stock'` และกันเติมซ้ำด้วย `restockId`
- กลับมาที่ออร์เดอร์เดิมหลังทำอาหาร
- จุดคำนวณ `restock`

เกณฑ์ผ่าน: ทำอาหารหนึ่งรอบแล้วสต็อกเพิ่มถูกต้องครั้งเดียว และยังอยู่หลัง reload

**สถานะ: เสร็จแล้วพร้อม Phase 1** (ยกเว้นจุดคำนวณ `restock` ซึ่งเป็นของระดับ 4)

### Phase 3: ตลาดและของตกแต่ง

- หน้าตลาด ป้ายราคา กระเป๋าเงิน จ่ายเงิน รับเงินทอน
- กล่องของตกแต่งและช่องวางในร้าน
- commit การซื้อ กันเงินติดลบ
- ใส่รูปจาก Sheet A/B

เกณฑ์ผ่าน: ขายได้เงิน → ซื้อของ → เห็นของในร้าน และค่าคงที่ของเงินเป็นจริงทุกครั้ง

**สถานะ: เสร็จแล้ว (2026-09-23, v33)**

- ทางเข้า: แตะกระปุกแมวน้ำในร้าน (มีถุงช้อปปิ้งเล็กๆ บอก) → ตลาด คนขายคือกระรอก
- ของแต่ง 10 ชิ้นราคา 5–20 บาท (`DECOR` ใน `core.js`) ซื้อได้ชิ้นละครั้ง แตะของที่มีแล้วเพื่อเก็บเข้ากล่อง/เอาออกมาแต่ง
- ระดับ 1: แตะเหรียญจ่ายอิสระ จ่ายเกินคนขายทอนให้เอง; ระดับ 2: จ่ายพอดี (มีคำใบ้ + โหมดช่วยเหมือนร้าน); ระดับ 3: สลับระหว่างจ่ายพอดี กับจ่ายเหรียญ 10/แบงก์ 20 แล้วเลือกคำตอบ "ต้องได้เงินทอนกี่บาท" จาก 3 ตัวเลือก (ครั้งที่สองมีเส้นนับ) แล้วคนขายวางเงินทอนพร้อมนับต่อ
- กระเป๋าเงินแตกเหรียญให้จ่ายพอดีได้เสมอเมื่อเงินพอ; เงินไม่พอ คนขายบอก "เก็บเงินอีก X บาทนะ" พร้อมรูปเหรียญที่ขาด
- ของที่ซื้อวางในร้านตามจุดประจำของแต่ละชิ้น (`.decor-<id>` ใน `css/shop.css`) อยู่หลังลูกค้า
- ปรับจากแผน: จุดคำนวณ "นับเงินทอนที่ได้รับ" ย้ายจากระดับ 4 มาไว้ระดับ 3 เพราะตอนนี้ร้านมีแค่ระดับ 1–3 และผู้ปกครองอยากให้ได้ฝึกรับเงินทอนตอนซื้อ

### Phase 4: ย้าย math engine

- `shop/column.js` รับ fixed problem
- แก้ตัวตั้งหลักเดียวและคำตอบหลักเดียว
- โหมด `independent`/`guided` และการตอบไม่ตรงตาม D1
- ข้อความไทย/อังกฤษ
- tests โจทย์บังคับทั้ง 4 ข้อ

เกณฑ์ผ่าน: engine ใช้โจทย์กำหนดเองได้และแสดงตัวทด/ตัวยืมถูกต้อง

### Phase 5: ระดับ 4–5

- บวกราคา 2 ชิ้น ของเหลือ
- ออร์เดอร์สองเมนู แบงก์ 20
- `8 + 7 = 15` และ `20 − 13 = 7` ใน flow จริง

### Phase 6: ระดับ 6 และหน้าผู้ปกครอง

- หน้าบริหารสต็อก
- สถิติผู้ปกครอง: ออร์เดอร์ ยอดขาย ทักษะที่ใช้ความช่วยเหลือบ่อย

### Phase 7: เชื่อมกลับโลกของลิลลี่

- **ทดสอบบน iPad จริงก่อน:** ถ้าติดตั้งลิลลี่ไว้บนหน้าจอหลัก แอปแบบนี้บน iOS มีที่เก็บข้อมูลแยกจาก Safari การเปิดครัวจากในลิลลี่อาจได้เซฟร้านคนละชุดกับตอนเปิดครัวเอง และการเปิด URL นอก scope อาจโผล่แถบ browser ถ้าเป็นแบบนั้นต้องตัดสินใจใหม่ก่อนทำต่อ
- เพิ่ม tile ร้านอาหารในห้องพักเล่น
- เปิด URL ของครัวพร้อม `mode=restaurant`
- ส่ง `return=/game-lilly/` แบบ whitelist path เท่านั้น
- ปุ่มกลับในร้านพากลับโลกของลิลลี่
- ไม่แชร์หรือเขียนทับ localStorage ของโลกของลิลลี่

ทุก Phase: ตรวจ responsive offline bump `sw.js` และทดสอบทั้ง local และ live URL

## 20. Automated test ที่ต้องมี

### Unit (`node --test`, ไม่ต้องเปิด Chrome)

- สร้างออร์เดอร์ไม่เกินสต็อก ของหมดไม่สร้างออร์เดอร์
- ทุกออร์เดอร์มีจุดคำนวณ 1 จุดพอดี และอยู่ในรายการของระดับนั้น
- การจ่ายเงินเป็นแบบจริง (พอดี หรือเหรียญ/แบงก์ใบเดียวที่มากกว่าราคา)
- ลำดับการนับต่อของการทอน รวมเหรียญถูกต้อง
- ทอนน้อยหรือเกินไม่ commit ทอนครบ commit ครั้งเดียว commit ซ้ำด้วย `order.id` เดิมไม่เกิดผล
- ค่าคงที่ของเงินเป็นจริงหลังขายและซื้อหลายรอบ
- ซื้อของเงินไม่พอไม่ commit และ `piggy` ไม่ติดลบ
- กระเป๋าเงินแตกเหรียญให้จ่ายพอดีได้เสมอเมื่อเงินพอ
- เติมสต็อกซ้ำด้วย `restockId` เดิมไม่เพิ่มซ้ำ
- `loadShop()` กับข้อมูลเสียหายไม่แตะ key ของครัว
- `buildSteps()` ของโจทย์บังคับทั้ง 4 ข้อ ตัวทดอยู่หลักสิบ ตัวยืมขีดเลขเดิม ไม่มีศูนย์นำหน้า

### E2E

- วงจรขายระดับ 1–3 ตั้งแต่ลูกค้าเข้าจนฉลอง
- ตอบไม่ตรงครั้งแรกแสดงคำใบ้ ครั้งที่สองเข้า guided ปุ่มขอความช่วยเหลือเปิด guided ทันที
- ไม่มี ✕ หรือสีแดงบนจอในร้าน
- เสียงสรุปจบก่อนปุ่มไปต่อเปิด
- ทำอาหารเพิ่มแล้วกลับออร์เดอร์เดิม
- reload ระหว่างออร์เดอร์แล้วกลับมาที่ขั้นเดิม
- ออร์เดอร์ไม่เปลี่ยนเมื่อเข้าและออกหน้าคำนวณ
- ตลาด: ซื้อ รับเงินทอน วางของตกแต่ง

### Regression

- เซฟครัวรูปแบบเก่ายังโหลดได้ และไม่มี field ร้านปนเข้าไป
- เซฟโลกของลิลลี่และ level ID เดิมไม่เปลี่ยน
- เกมทำอาหารครบทุกเมนูยังจบได้
- หน้าเสิร์ฟและการป้อนเพื่อนเดิมยังทำงาน
- สมุดผลงาน เพื่อนปลดล็อก และออร์เดอร์รายวันเดิมยังทำงาน

### หน้าจอและอุปกรณ์

- 390 × 664, 390 × 844, 768 × 950, 834 × 1100, 1024 × 660, 1280 × 900
- ไม่มี horizontal overflow และ home ของครัวยังไม่ต้องเลื่อนที่ 390 × 664
- ไม่มี text selection, copy menu, browser image drag, pinch zoom หรือ double-tap zoom
- เสียงพูดและเสียงเครื่องครัวยังดังบน iPad/iPhone Safari
- Phase 7: ทดสอบลิลลี่แบบติดตั้งบนหน้าจอหลัก → เปิดร้าน → กลับ

## 21. เกณฑ์รับงาน

### ชิ้นแรก (Phase 1)

- เปิดร้านครั้งแรกมีคุกกี้บนชั้นและลูกค้ามาทันที
- เพื่อนเข้าร้านและสั่งสินค้า 1 เมนูได้ เด็กหยิบ 1–5 ชิ้นได้
- รับเงินพอดีและทอนแบบนับต่อได้ ทอนน้อย/เกิน/ครบมีผลตอบรับต่างกันตาม D1
- ทุกออร์เดอร์มีจุดคำนวณเดียว ลูกค้าหนึ่งคนจบในราว 1–1.5 นาที
- ขายสำเร็จแล้วตัดสต็อกและเพิ่มเงินในกระปุกเพียงครั้งเดียว
- ออร์เดอร์อยู่ครบหลัง reload
- เสียงอ่านจบก่อนดำเนินขั้นต่อไป
- เกมเดิมและเซฟเดิมทั้งหมดผ่าน regression tests

### MVP เต็ม (ถึง Phase 3)

- เพิ่มจากชิ้นแรก: ทำอาหารเพิ่มแล้วเข้าสต็อก ไปตลาดซื้อของตกแต่งด้วยเงินที่ขายได้ รับเงินทอน และเห็นของตกแต่งในร้าน

## 22. สิ่งที่อยู่นอก MVP

- เงินสตางค์หรือราคาทศนิยม
- การคูณและหาร
- แบงก์ 50/100 และยอดเกิน 20 บาท
- เกม "จับผิดเงินทอน" (คนขายทอนผิดให้เด็กหา) — อาจสับสนสำหรับเด็กเล็ก รอดู feedback ก่อน
- ขายของตกแต่งคืน หรือวางของตกแต่งแบบอิสระ
- ระบบหลังบ้านซับซ้อน cloud sync หรือบัญชีผู้ใช้
- การซื้อเงินจริง โฆษณา หรือ subscription
- คะแนนติดลบ การลงโทษ หรือการทำให้ลูกค้าผิดหวังรุนแรง
- รูปตัวละครอารมณ์ใหม่ หาก CSS และสัญลักษณ์ยังสื่อสารได้เพียงพอ

## 23. วิธีทำงานสำหรับ AI ผู้รับช่วง

1. อ่านหัวข้อ 0 ของไฟล์นี้ แล้วอ่าน `MEMORY.md` และไฟล์ handoff ของทั้งสอง repo ก่อนแก้
2. ตรวจ `git status` และห้ามย้อนการเปลี่ยนแปลงที่ไม่ได้สร้างเอง
3. รัน tests เดิมเพื่อเก็บ baseline
4. ทำทีละ Phase ให้ tests ของ Phase นั้นผ่านก่อนเริ่ม Phase ถัดไป และหลัง Phase 1 ต้องรอ feedback จากการเล่นจริง
5. ใช้ระบบเสียง รูป ตัวละคร และ interaction เดิมก่อนสร้างของใหม่
6. ห้ามแตะ localStorage key ของครัวและโลกของลิลลี่ ข้อมูลร้านอยู่ key แยกเท่านั้น
7. ห้ามเปลี่ยน normal cooking/serve flow เพื่อให้ร้านทำงาน
8. แก้ไฟล์ข้อความไทยด้วย Python/sed/Write เท่านั้น ห้ามใช้ PowerShell `Get-Content`/`Set-Content` (ทำให้ UTF-8 ภาษาไทยเสีย)
9. หลังแก้ไฟล์ shell ต้อง bump `?v=` ใน `index.html` ชื่อ cache และ URL ใน `sw.js` และทุก dynamic import
10. หลังเปลี่ยนข้อความไทย ต้องรัน `design/voice.py` ใหม่
11. ทดสอบ local และ live URL ก่อนปิดงาน
12. สรุปสิ่งที่ทำ ไฟล์ที่แก้ migration และผล tests ทุกครั้ง
